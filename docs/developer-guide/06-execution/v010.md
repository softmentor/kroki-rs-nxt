---
title: v0.1.0 Execution Tracker
label: kroki-rs-nxt.developer-guide.execution.v010
---

# v0.1.0 Execution Tracker

## Purpose

Track concrete delivery tasks, current status, and required updates for the `v0.1.0-alpha` development stream.

Date baseline: **2026-03-03**.

---

## Current Snapshot

### Repository State

- Rust workspace baseline is active and includes `core`, `adapters`, `apps/cli`, and `apps/server`.
- TypeScript-facing surfaces have bootstrap baseline packages with lint/build/test wiring.
- Documentation is now reorganized into numbered developer-guide flows.

### In Scope for This Tracker

- Phase 2 bootstrap completion
- Preparation for Phase 3 provider migration
- Documentation and execution hygiene

---

## Workstreams

## 1) Core Domain Foundation

Status: **In Progress**

- [x] `core/sdk-rust` bootstrap baseline modules
- [x] Base domain types and trait signatures present
- [x] Finalize stable request/response and error contracts for migration batches
- [ ] Define capability registry and provider metadata contract

## 2) Adapter Foundations

Status: **In Progress**

- [x] `adapters/storage` bootstrap baseline crate
- [x] `adapters/transport` bootstrap baseline crate
- [x] Implement initial HTTP transport interfaces and DTO mapping
- [ ] Implement first cache adapter contract against core ports

## 3) App Surfaces (CLI + Server)

Status: **In Progress**

- [x] CLI and server entry points with `lib.rs` + `main.rs` split
- [x] Basic startup/logging behavior present
- [x] Wire CLI commands to core service layer
- [x] Wire server routes to adapter and core orchestration
- [ ] Add minimal health/readiness behaviors with structured responses

## 4) TypeScript Surfaces and Shared SDK

Status: **Planned**

- [x] `core/sdk-ts` bootstrap baseline package artifacts
- [ ] Define Wasm boundary API for first exported operations
- [x] `apps/web-app` and `apps/vscode-ext` bootstrap baseline package internals
- [x] `apps/myst-plugin` bootstrap baseline package internals and rendering entry points
- [x] Introduce initial shared package baseline in `shared/design-system`

## 5) Quality and Delivery Pipeline

Status: **In Progress**

- [x] `devflow.toml` baseline target profiles present
- [x] Confirm `dwf check:pr` green on local reproducibility path
- [x] Confirm GitHub workflow includes cache-aware host/container parity execution
- [ ] Reconcile custom CI workflow with `dwf ci:check` generator expectations (or keep documented drift by design)
- [x] Add initial integration test structure by crate
- [ ] Define smoke-test scope for Phase 2 exit gate

## 6) Documentation and Architecture Governance

Status: **In Progress**

- [x] Developer guide flow reorganized (01/02/03/04/06/10)
- [x] Root duplicate docs migrated and removed
- [x] Roadmap and migration docs linked into execution flow
- [ ] Add first phase-level execution update log entry after next engineering increment
- [ ] Add explicit ownership tags once team roles are finalized

---

## Phase 2 Exit Checklist (Operational)

- [x] `cargo check --workspace` passes
- [x] `dwf check:pr` passes
- [x] Core port boundaries documented and validated in code
- [x] CLI and Server stubs upgraded to first functional vertical slice
- [x] Documentation aligned with actual code topology

---

## Immediate Next Updates Required (Phase 3 Kickoff)

1. Start Batch 3.1 command-provider migration (Graphviz/D2) on top of frozen contracts.
2. Define and implement capability registry + provider metadata contract in `kroki-core`.
3. Expand transport error/status mapping conventions for additional providers.
4. Add smoke-test scope for server critical path and include it in `main` target verification.

---

## Phase 2 Closure Record

Date: **2026-03-03**

Phase gate decision:

- **Decision**: Phase 2 closed, Phase 3 authorized to start.
- **Evidence**:
  - Vertical slice landed (`request -> provider stub -> output contract`) through core + adapter + app.
  - Core contract boundaries documented and frozen for `v0.1.0-alpha`.
  - Phase 3 provider migration backlog documented with dependency ordering and risk notes.
  - `cargo check --workspace` and `dwf check:pr` passing.

Ownership tags (initial role-based owners):

- Core/Provider owner: **Core Platform Lead**
- Adapter/Transport owner: **Runtime Surfaces Lead**
- CI/Test owner: **Developer Productivity Lead**
- Documentation owner: **Documentation Maintainer**

---

## Change Log

### 2026-03-03

- Initialized `v0.1.0` execution tracker.
- Added baseline task matrix across core, adapters, apps, TS surfaces, quality, and documentation.
- Linked execution tracking to roadmap and developer guide navigation.


### 2026-03-03 (Foundation Readiness Update)

- Completed bootstrap baseline for Rust and TypeScript workspaces with lint/build/test wiring.
- Enabled `test:integration` in PR target profile.
- Added fixture and resource handling conventions with working examples.
- Generated CI workflow and then customized it for host/container parity with cache and registry optimizations.

### 2026-03-03 (Phase 2 Remaining Tasks Reconciled)

- Revalidated `cargo check --workspace` and `dwf check:pr` for current bootstrap baseline.
- Aligned immediate-next task list to explicit Phase 2 closure requirements before Phase 3 transition.

### 2026-03-03 (Phase 2 Closure Decision)

- Landed and validated minimal vertical slice across core, transport adapter, CLI, and server surfaces.
- Added frozen contract documentation for `DiagramRequest` / `DiagramResponse` / `DiagramError` boundaries.
- Added explicit Phase 3 provider migration backlog with batch sequencing and risk notes.
- Recorded formal phase gate decision and initial ownership tags.
